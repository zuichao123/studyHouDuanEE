synchronized可以修饰实例方法，也可以修饰静态方法，还可以修饰代码块，会为代码块加上内置锁，从而实现线程同步。

在静态方法中添加同步代码块。

synchronized修饰同步代码块，

synchronized(){

//

}

()内部需要设置加锁的资源，静态方法属于类的方法，不属于任何一个实例对象，所以静态方法中的synchronized()只能锁定类，不能锁定实例对象，this可以表示当前的一个实例。

实例方法中也可以使用synchronized关键字实现同步代码块。